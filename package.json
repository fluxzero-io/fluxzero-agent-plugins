{
  "name": "fluxzero-agent-plugins",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-plugins.mjs",
    "check:generated": "node scripts/generate-plugins.mjs --check",
    "validate": "node scripts/validate-plugins.mjs",
    "test": "npm run check:generated && npm run validate && node --test scripts/*.test.mjs"
  }
}
